" BUNDLES PLUGINS:
" Include all the bundled plugins this distribution is dependent on.
set nocompatible            " Harnest the iMprovements of Vim over Vi
filetype off                " Turn off file type detection.
set rtp+=~/.vim/bundle/vundle/
call vundle#rc()
Bundle "gmarik/vundle"


" "File Type Detection"
filetype plugin indent on         " Automatically detect file types.
